package org.usfirst.frc.team5892.robot.commands.auton;

import edu.wpi.first.wpilibj.command.Command;
import edu.wpi.first.wpilibj.command.CommandGroup;

public class &name implements AutonBuilder {

    @Override
    public Command buildAuto(String fieldData) {
        return new &nameCG(fieldData);
    }

    private class &nameCG extends CommandGroup {
        &nameCG(String fieldData) {

        }
    }
}
